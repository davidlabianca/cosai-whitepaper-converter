{
	"name": "CoSAI Whitepaper Converter",
	"dockerFile": "Dockerfile",

	"features": {
		"ghcr.io/devcontainers/features/docker-in-docker:2": {}
	},

	"onCreateCommand": "./scripts/install-deps.sh && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt && npm install",

	"customizations": {
		"vscode": {
			"extensions": [
				"charliermarsh.ruff",
				"ms-python.python"
			],
			"settings": {
				"python.defaultInterpreterPath": "${workspaceFolder}/.venv/bin/python"
			}
		}
	},

	"remoteUser": "vscode"
}
